This project demonstrates the use of Support Vector Machine (SVM) for classifying the Iris dataset using Python and Scikit-learn.  
The notebook includes data visualization, model training, prediction, evaluation, and hyperparameter tuning using GridSearchCV.  
The final model achieved high classification accuracy on the test dataset.
